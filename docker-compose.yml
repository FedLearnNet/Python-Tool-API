services:
  python-app:
    build:
      context: .
      dockerfile: Dockerfile.local
    container_name: python-training-app
    environment:
      - PYTHONUNBUFFERED=1
      - WS_URL=ws://localhost:8080/model/
      - HTTP_URL=http://localhost:8080/model/
      - APP_ID=12341234afdsa
      - APP_KEY=12341234afdsa
