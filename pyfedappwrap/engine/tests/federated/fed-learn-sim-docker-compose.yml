services:
  controller:
    # TODO: change from tmptag to actual tag after merging into develop
    image: gitlab.cosy.bio:5050/cosybio/federated-learning/federated_db/feature-cloud-controller/controller:tmptag
    ports:
      - 8002:8000 # orchestration http
      - 8003:8001 # app comm http
    environment:
      - LOG_LEVEL_STDOUT=debug
      - LOG_LEVEL_FILE=debug
      - RELAY_ADDRESS_TCP=relay:9141
    healthcheck:
      # This uses bash to send a raw GET request to the local port
      test: [ "CMD-SHELL", "bash -ec 'exec 3<>/dev/tcp/127.0.0.1/8000; printf \"GET /healthz HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n\" >&3; grep -q \"200 OK\" <&3'" ]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 5s
    networks:
      - fedlearn-sim-network


  relay:
    image: gitlab.cosy.bio:5050/cosybio/federated-learning/federated_db/feature-cloud-controller/controller-relay:tmptag
    ports:
      - 9141:9141 # tcp port
      - 9140:9140 # http port
    environment:
      - LOG_LEVEL_STDOUT=debug
      - LOG_LEVEL_FILE=debug
    healthcheck:
      # This uses bash to send a raw GET request to the local port
      test: [ "CMD-SHELL", "bash -ec 'exec 3<>/dev/tcp/127.0.0.1/9140; printf \"GET /healthz HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n\" >&3; grep -q \"200 OK\" <&3'" ]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 5s
    networks:
      - fedlearn-sim-network
networks:
  fedlearn-sim-network:
    driver: bridge
    name: fedlearn-sim-network
